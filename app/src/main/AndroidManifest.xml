<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gunderson.wgu.org.wguapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainLanding">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TermList" />
        <activity android:name=".CourseList" />
        <activity android:name=".CourseDetails" />
        <activity android:name=".MentorDetails" />
        <activity android:name=".MentorList" />
        <activity android:name=".AssessmentDetails" />
        <activity android:name=".Notes" />
        <activity android:name=".ShareNotes" />
        <activity android:name=".TermDetails" />
        <activity android:name=".NotesList" />
        <activity android:name=".AssessmentList" />
    </application>

</manifest>